Code shouldn't rely on in-code variables (besides local function one-time use variables). Use attrribute as shown in code
Code relies purely on functions.
GUI not yet made as it's supposed to be all-device compatible, draggable, and *hopefully* not break if re-loadstring-ed
